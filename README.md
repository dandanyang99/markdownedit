# Markdown 编辑器

一个跨平台的 Markdown 编辑器，具有实时预览和便捷的工具栏功能。

## 功能特性

- 实时 Markdown 预览
- 便捷的工具栏，一键插入 Markdown 语法
- 支持加粗、斜体、标题、链接、图片、列表等常用语法
- 显示行数和字符数统计
- 跨平台支持（Windows、macOS、Linux）

## 安装与运行

1. 确保已安装 Node.js（版本 12.x 或更高）

2. 安装项目依赖：
   ```bash
   npm install
   ```
   
   如果在国内网络环境下安装失败，可尝试使用国内镜像源：
   ```bash
   npm install --registry https://registry.npmmirror.com
   ```

3. 运行开发模式：
   ```bash
   npm start
   ```

4. 或者双击运行 start.bat 文件启动应用

## 故障排除

如果在安装过程中遇到 EBUSY 或权限错误：

1. 关闭所有 Node.js/Electron 相关进程
2. 删除 node_modules 目录和 package-lock.json 文件
3. 重新运行 `npm install`

如果遇到网络超时错误，使用国内镜像源安装依赖。

## 工具栏功能

- **B**: 插入加粗语法 `**加粗文字**`
- **I**: 插入斜体语法 `*斜体文字*`
- **H**: 插入标题语法 `### 标题`
- **🔗**: 插入链接语法 `[链接描述](https://)`
- **🖼️**: 插入图片语法 `![图片描述](https://)`
- **•**: 插入无序列表 `- `
- **1.**: 插入有序列表 `1. `
- **{}**: 插入内联代码语法 `` `代码` ``
- ****{}**: 插入代码块语法:
  ```
  ```
  代码块
  ```
  ```
- **❝**: 插入引用语法 `> `

## 技术栈

- Electron: 构建跨平台桌面应用
- React: 用户界面
- Marked: Markdown 解析
- DOMPurify: HTML 内容净化

## 构建

要构建生产版本的应用程序，请运行：

```bash
npm run build
```

## 开发

要进行开发，请运行：

```bash
npm run dev
```